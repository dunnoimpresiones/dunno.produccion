# DUNNO Producción — React + TypeScript + Vite + Tailwind

Frontend reconstruido fielmente a la referencia visual. Incluye selector de colores multiselección que permanece abierto hasta pulsar “Listo”.

## GitHub Pages
El workflow de `.github/workflows/pages.yml` compila y publica automáticamente al hacer push a `main`.

> El backend Apps Script no debe subirse a este repositorio público.
